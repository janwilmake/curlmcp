{
  "name": "curlmcp",
  "version": "2.0.0",
  "description": "curl MCP with X OAuth - the last MCP you'll need",
  "main": "main.ts",
  "dependencies": {
    "simplerauth-client": "^0.0.14",
    "with-mcp": "^0.0.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250906.0"
  },
  "scripts": {
    "dev": "wrangler dev --env=\"\"",
    "deploy": "wrangler deploy --env production"
  }
}
